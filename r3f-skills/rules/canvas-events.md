# canvas-events

---

title: Configure event handling.
impact: HIGH

---

```jsx
<Canvas
  events={undefined} // Use default DOM events
  eventSource={document.body} // Event source element
  eventPrefix="offset" // 'offset' | 'client' | 'page' | 'layer' | 'screen'
>
```
